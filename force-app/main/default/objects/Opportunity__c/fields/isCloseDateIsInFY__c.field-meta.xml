<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>isCloseDateIsInFY__c</fullName>
    <formula>AND(
    Close_Date__c  &gt;= DATE(YEAR(TODAY()) - IF(MONTH(TODAY()) &lt; 4, 1, 0), 4, 1),
    Close_Date__c  &lt;= DATE(YEAR(TODAY()) + IF(MONTH(TODAY()) &gt;= 4, 1, 0), 3, 31)
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>isCloseDateIsInFY</label>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
